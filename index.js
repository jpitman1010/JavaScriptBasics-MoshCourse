let name = 'Julie';  //string literal
let age = 30; //number literal
let isApproved = true; //boolean literal
let firstName = undefined; //undefined literal
let selectedColor = null; //null literal - 
//use if user doesn't have favorite color, it will
//return null, but if they should choose a fav color
//it can be changed to the color they choose.

